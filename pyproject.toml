[project]
name = "flashbulljev"
version = "0.4.0"
description = "flashbulljev - fast and stored System One: typed decisions with certified cache"
requires-python = ">=3.11"
dependencies = ["numpy>=1.24", "fastapi>=0.110", "uvicorn>=0.29", "pydantic>=2.0", "requests>=2.31"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[build-system]
requires = ["setuptools"]
